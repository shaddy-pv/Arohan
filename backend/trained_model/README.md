# Trained model files are generated at runtime.
#
# These binary files are gitignored (.pkl, .caffemodel, .prototxt).
# They will be auto-downloaded or generated when you first run:
#
#   python start_cv_backend.py
#
# See docs/BACKEND.md for details.
